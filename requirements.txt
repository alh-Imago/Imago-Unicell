# Imago UniCell — Python dependencies

# Core (required for the VM and compiler, nano/)
# No mandatory third-party dependencies for the DSL/compiler/VM/
# introspection/AI-port/workbench modules -- they run on the Python
# standard library alone.

numpy               # required by nano/gpu_array_v1.py (#216 item 3's
                     # own CPU-fallback array backend); imported
                     # unconditionally there, not truly optional

# Development / testing
# pytest             # for tests/vm/test_*.py

# Optional
# cupy               # only used if a real CUDA device is available;
#                      nano/gpu_array_v1.py falls back to numpy
#                      automatically otherwise
