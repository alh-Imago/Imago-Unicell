# Session — 2026-05-09 (Full Day)
## Tier 2 Complete · Tier 4 Complete · Docs · Composer

---

## HARDWARE STATUS

| Item | Cost | Order | ETA | Status |
|------|------|-------|-----|--------|
| Kintex-7 XC7K480T board | £54.90 | 22-14594-85183 | 2 Jun – 6 Jul | IN TRANSIT |
| JTAG SMT2 programmer | £25.74 | 23-14593-40180 | By 21 May | IN TRANSIT |
| Vivado ML Standard | Free | N/A | — | Downloading |

---

## WHAT WAS DONE

### 1. fp_tiles.py — Tier 2 final item

**NOR2 eliminated (last v1 remnant):**
- `COUNTER_DECREMENT` zero-detector: NOR tree → OR2 tree + NOT (native v2)
- `SR_LATCH`: NOR2(a,b) → NOT(OR2(a,b))

**INT32_SUB upgraded:**
- Was: ripple-carry depth 65, 192 cells
- Now: Kogge-Stone depth 12, 517 cells

**GS_OUT_POSEDGE on all NORBuilder emissions:**
- `_emit`, `_emit2`, `_emit_v2` all OR in GS_OUT_POSEDGE
- Safe default: output releases posedge N+1

### 2. ir.py — GS_OUT_POSEDGE on all compiler-emitted cells

All paths in `lower_to_cell_map_v2` now set GS_OUT_POSEDGE.

### 3. model_library.py — FP32 estimates → actuals

| Model | Old | New (actual) |
|-------|-----|-----|
| FP32_ADDER | 3,000 / depth 40 | **1,253 / depth 85** |
| FP32_MULTIPLIER | 35,000 / depth 80 | **3,066 / depth 89** |
| INT32_SUBTRACTOR | 580 / depth 13 | **517 / depth 12** |

### 4. pond_types.py + pond.py — Tier 2 threshold migration

`PondTypeSpec` now owns `stall_threshold` and `anomaly_threshold`.
Per-type values: DEVICE=15 cycles, PROCESS=100, FILE=200, COMPANION anomaly=20%, etc.
`Bridge.__init__` reads from registry, falls back to 50/50.0.

### 5. OR lowering confirmed (Tier 4)

Depth gaps 0/1/3/5 all produce correct OR results. SYNC_WAIT is 1 cell in v2.
`test_gate_state_32.py` updated: SYNC_WAIT 1 record not 3, depth max+1 not max+2.

### 6. Compiler constant auto-injection (Tier 4)

`compile_function()` populates `self.known_values: {bus_addr: val}`.
`load_map()` accepts `known_values=`, stores on Region.
`start()` auto-injects before user inputs.
Updated: test_compiler.py, compiler.py, compiler_int32.py, program_builder.py.
Verified: `always_true(a) = a and 1` works without manual constant injection.

### 7. Workbench UI — two-input display (Tier 4)

Inspector: `Input addr` → `Input A addr`; conditional `Input B addr` and
`Input B val` rows; `Two-input` row showing `A↑ B↓`.
Grid: two-input cells get small accent dot (CSS `::after`) top-right corner.

### 8. README.md — complete rewrite

Accurate status table, silicon validation results, all three variant test counts,
v2 gate function table, tile library with actual figures, portability table,
key concepts, repository structure. Founding idea section with NOR universality.

### 9. docs/RUNNING.md — new file

Full workflow guide. Composer → .icm → VM → FPGA.
Python APIs for VM loading (raw and ProgramImage named-range).
Compile-from-source examples. FPGA bridge API. Full pipeline example.
Variant selection, 6-stage bring-up sequence, requirements, file location table.

### 10. Composer — FPGA target selector + cell budget + vmOnly warnings

**Target selector (toolbar):**
VM (unlimited), iCEBreaker 64, iCEstick 16, Basys3/Arty 256, OrangeCrab 256,
Kintex-7 1500, Custom N. Switching rebuilds library and updates budget bar.

**Cell budget bar (statusbar):**
Shows cost/budget(%) live. Amber at 80%, red when over. Hidden on VM.

**Model library — accurate figures:**
- INT32_ADDER: 482 cells depth 2 (was 96/12)
- INT32_SUBTRACTOR: 517 cells depth 12 (was 580/13)
- FP32_ADDER: 1,253 cells depth 85 (was 3,000/40 estimate)
- FP32_MULTIPLIER: 3,066 cells depth 89 (was 35,000/80 estimate)
- New: INT32_NOT, INT32_AND, INT32_OR, INT32_XOR (32 cells, depth 1)

**vmOnly flag:**
Models too large for the selected target get amber border + `VM only` badge.
Dynamically applied when model cells > target budget.
Warning row: "⚠ exceeds N cell budget — VM / large FPGA only"

**.icm export:**
Embeds `target`, `cell_budget`, `vm_only` fields.
Confirm dialogs when budget exceeded or VM-only models in design on FPGA target.

---

## TIER STATUS

| Tier | Status |
|------|--------|
| Tier 1 — v1 retirement | ✅ Complete |
| Tier 2 — OS layer v2 migration | ✅ **Complete** |
| Tier 3 — Silicon features (ECC etc.) | Deferred to hardware |
| Tier 4 — Architecture refinements | ✅ **Complete** |
| Tier 5 — VM package | 🔜 **Next session** |
| Tier 6 — Docs | ✅ Substantially complete (README + RUNNING.md) |

---

## TEST BASELINE — END OF SESSION

```
Main (standard): 2,329 passed / 6 failed  (all pre-existing deprecated)
unicell-latch:   2,535 passed
unicell-edge:    2,326 passed / 9 failed   (all pre-existing)

Total across all variants: ~7,190 passing
```

Zero new regressions introduced this session.

---

## COMMITS THIS SESSION

```
768fb72  Tier 2 complete + Tier 4 progress: fp_tiles v2, GS_OUT_POSEDGE, pond thresholds
e149596  Session log 2026-05-09
b0d0add  Tier 4 complete: compiler constant injection + workbench two-input display
8b9349c  Docs: README rewrite + RUNNING.md workflow guide
55691f9  Session log update 2026-05-09 (continued)
8eb71b7  Composer: FPGA target selector, cell budget, vmOnly model warnings
```

---

## NEXT SESSION — Tier 5: Standalone VM Package

### Goal

`pip install imago-vm` — anyone with Python 3.10+ can run UniCell programs
without hardware, without cloning the repo, without reading the source.

### Estimated scope (one session)

1. `pyproject.toml` — package metadata, dependencies, entry points
2. Package structure: `imago/` namespace, decide what's core vs optional
3. Entry points:
   - `imago-workbench` → launches workbench browser UI
   - `imago run <file.icm>` → runs a program, prints output
   - `imago compile <source>` → compiles and prints cell map
4. Bundled example `.icm` programs (NOT gate, AND, adder, for loop)
5. `pip install -e .` test, then real install test
6. Update RUNNING.md with pip install instructions

### Out of scope for v1

- numpy performance mode (do after user feedback)
- VM vs silicon diff tool (needs hardware)
- PyPI upload (manual step, do it once package builds clean)

### Session start note for next assistant

All Tiers 1–4 complete. 2,329 tests passing. The repo IS the memory — pull it,
read sessions/latest.md, trust it. Today's work is entirely in Tier 5.
The code is already correct — this is a packaging and distribution task.
