# current/

The three live documents, kept together per the 2026-08-04 archeology
sweep (see `../archeology/README.md` for the full reorganization):

- **`START.md`** — session catch-up checklist, read first each session.
- **`PLAN.md`** — the active task path.
- **`latest.md`** — fast catch-up on the most recent session's state
  (most recent at top). Dated/archived session history now lives in
  `../archeology/sessions/`.

Everything else that used to live loose at the repo root or in `docs/`
has moved to `../archeology/`, split by which cell line it actually
describes (FULL cell / stripped cell / shared).
