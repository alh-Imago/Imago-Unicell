# paper_robotics

## Paper 5 — Deterministic sensor-to-actuator pipeline



Data: SensorTrix tile counts/depths, PID pipeline latency (predicted + measured),
      real sensor readings once hardware arrives, cascade PID convergence traces.
Figures: SensorTrix -> OptiTrix -> actuator pipeline diagram, latency comparison
         vs CPU/MCU equivalent, sensor array addressing illustration.

## Structure

```
draft.md        ← working draft
notes.md        ← scratch, open questions, reviewer notes
data/           ← raw measurements, CSV, JSON, timing logs
figures/        ← plots, diagrams, SVG/PNG (generated from data/ where possible)
refs.bib        ← BibTeX references for this paper
```

Status: see `papers/PAPERS.md`
