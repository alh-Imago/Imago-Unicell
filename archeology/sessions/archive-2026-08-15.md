# Session Archive — 2026-08-15

**Full narrative for `points.md` #301-317. See `current/latest.md` for
the compressed current-state summary; this file is the story of how
today's session unfolded.**

## Opening: RAM-side memory questions (#301/#302)

Alan raised a genuinely new mechanism, sideways from the queued BRAM
circular-buffer plan: letting a CELL supply the RAM address (not just
`addr_counter_v1.v`), giving real random-access read/write. This
creates a stall hazard (a downstream chain stalled means counter-fed
data is lost or the whole system backs up) — resolved with a retry
loop feeding a new priority-arbitration core, made cheap by RAM's own
non-destructive read (only the ADDRESS needs stashing, not the data).
A real out-of-step staleness hazard was flagged, structurally similar
to `#37`'s already-accepted staleness window but not obviously bounded
the same way. `#302` sharpened Alan's own multi-chain out>in worry with
a concrete case grounded in the real, already-confirmed tree-system
topology (`#273`/`#286`): write traffic scales with join COMBINATIONS,
read traffic scales with chain COUNT — a real structural asymmetry.
Both entries logged as speculative direction, no RTL, needs real
testing before trust.

## Recovering a lost thread (#303-305)

Alan shared a full transcript of a prior conversation that had reasoned
through the FULL cell's real shift/lane mechanisms, the fat-unicell
direction, and a longer-horizon dev-tool vision — ending with an
explicit "yes please note it" that never actually reached `points.md`.
Only the MAN-file/compiler thread from that same conversation had been
logged (`#300`). Recovered and logged properly: `#303` verified the
shift/lane RTL fields directly against `unicell64_v3.v` and named the
real Path A/B fork (merge into one core vs. keep as a separate
selected block). `#304` captured the fat-unicell/"super carrier shell"
direction itself — one cell holding every core, selected by
configuration, directly reversing `#263`'s own logged ICM-portability
collapse. `#305` captured the three real jobs the separately-developed
cores keep doing even after a fat unicell exists (proof, independent
development, future dev-tool building blocks), correctly distinguished
from `#300`'s own MAN-file/BINDER lineage rather than double-logged as
the same idea. A real process gap was named directly: nothing enforces
that everything approved for logging in an exploratory conversation
actually gets logged before that conversation ends.

## Testing: root-causing #298 (#306-308)

Alan chose to start with testing. Direct simulation (a purpose-built
debug testbench instrumenting internal hierarchy) showed `#298`'s
"intermittent, pass-3-only" bug was actually TWO distinct, fully
deterministic bugs, not one intermittent case — a real correction to
`#298`'s own account, made honestly rather than silently. `#306`: a
config-race in `S_CFGWAIT` sampling stale `cfg_step` as a level,
landing the genuine reconfiguration pulse on top of the first feed of
EVERY pass (not just pass 3) — fixed with an edge-triggered `cfg_done_
pulse`. `#307`: a second, completely separate bug found only because
measurement continued after the first fix — a fixed collect-count of 3
stopped guaranteeing the accumulator landed below the comparator's
threshold once `#298`'s own varying-`feed_target` mechanism pushed
past 10. Every cell was behaving exactly as configured; the TEST was
wrong. Both fixed in `top_sentinel_discrete_test_v2.v` (`v1` retained
as the historical bug record), confirmed clean to 34,000+ passes.
`#308`: Alan's own real Quartus build came back with phantom-clock
figures — diagnosed directly as the already-documented missing-SDC
trap (`#171`/`#241`), fixed by adding `top_sentinel_discrete_test_v2.sdc`
matching the project's standard template. Re-run: Flow Status
Successful, `clk_div` 272.26 MHz, no failing paths, ALM unaffected by
the SDC fix (78 vs 75) — confirming yet again the established pattern
that ALM counts are never corrupted by the missing-SDC issue, only
timing verdicts are.

## Core-porting: the FULL cell audit and the first real addons (#309-313)

A full, ground-truth-verified audit of `unicell64_v3.v` against the
nano line, closing the long-queued "Cell Mechanics Deep Dive" from
`current/PLAN.md` (`#309`). Sorted the real port candidates into
already-ported, genuine candidates, suspect/unverified (three fields
`current/PLAN.md`'s own queue had already flagged as never once
exercised), and structurally-incompatible. Alan's own real
architectural catch (`#310`): the 6 genuine candidates split cleanly
along the SHELL/CORE boundary — 4 are pure data-path wrappers
(addon-shaped: shift, lane, nibble mask, invert), 2 change the cell's
own capture/firing state (core-shaped: `latch_in`, `latch_A_dis`),
argued Path B even more strongly for the addon-shaped four. `#311`:
the first three real ADDONs built anywhere in the nano line —
`shift_lane_addon_v1.v`, `nibble_mask_addon_v1.v`, `invert_addon_v1.v`
— faithfully ported and sim-verified (33+12+4 checks, all passing), a
real correction caught BEFORE building (lane-cut is coupled to
shift-OUT only, not an independent mechanism, contrary to earlier
framing). Full existing regression suite re-run and confirmed clean.
`#312`: a real A/B comparison design, `top_stripped_zone50_addons_v1.v`,
built at the same proven 50-cell scale as `#148`'s own baseline
(deliberately avoiding `#228`'s own lesson about Quartus pruning small
builds). `#313`: exact 20-bit addon-config-latch accounting, a
proposed `ADDON_LATCH[19:0]` bit layout, and a direct comparison
against the FULL cell's own original allocation (21 bits) for the same
mechanisms — this version is 1 bit leaner. A wrong claim about the
FULL cell's shift-enable priority was caught and corrected before
commit.

## The super carrier shell and the ICM question (#314-317)

Alan asked for a real comparison against the ICM file system. Traced
precisely: THREE generations of cell design exist, and the current
`.icm` format is grounded in the OLDEST one (`unicell.v` v2.3,
iCEBreaker era) — never updated for the FULL fat unicell's (`unicell64_
v3.v`, v3.1) own real capability expansion, a gap that predates this
whole session (`#314`). The sharper finding: heterogeneous,
individually-selectable core types never existed in ANY prior
generation — the fat-unicell direction isn't restoring something lost,
it's genuinely new territory. Alan's own real refinement (`#315`):
cores are mutually exclusive (config-time selectable, one active at a
time), meaning the shared core-config latch only needs sizing to the
WIDEST single core's requirement — a union, not a struct. Measured
directly from every real core's own `cfg_data` usage: 42 bits (RAM,
widest), not the naive 124-bit sum of all six — a real, load-bearing
number, not an estimate. Total real footprint: ~78 bits, leaner than
gen-2's 128-bit total while covering strictly more real capability
than gen-2 ever had. `#316`: Alan's real Quartus data for the addon-
augmented zone50 build — Flow Successful, 21,037 ALM, 8,463 registers,
`clk_div` 94.73 MHz. Deliberately NOT compared against `#149`'s own
original zone50 baseline — flagged honestly as untrustworthy for this
comparison (predates the SDC-discipline fix by 5 days, and its own
16.26 ALM/cell figure doesn't match any other trusted reference in
this project). `#317`: a real requirement captured before session
close — the core-type selector must stay genuinely extensible, built
to accept cores discovered later, not narrowly sized to today's 6.

## Session close

Alan confirmed the addon-build timing is comfortably good (`clk_div`
94.73 MHz against the real 25 MHz requirement, ~4x headroom) and set
the next-session order: rebuild the plain `top_stripped_zone50_v1`
baseline fresh, same session, same SDC discipline, so a genuinely
trustworthy delta against `#312`'s addon build can finally be
computed — then move to building the super carrier shell itself
(core-type selector, `#315`'s union-sized latch, `#317`'s
extensibility requirement).
