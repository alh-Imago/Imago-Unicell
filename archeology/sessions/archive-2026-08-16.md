# Session Archive — 2026-08-16

**Full narrative for `points.md` #325-335. See `current/latest.md` for
the compressed current-state summary.**

## Opening: recovering another lost thread, and closing real gaps

Alan asked whether a "chain header" on the Mustang cards would be
worth pursuing. Checking properly first: the multi-card interconnect
question had already been closed in a prior conversation, with real
specifics (no external transceiver breakout, the Dell Precision 5820
target, the rejected cheap-backplane option) — but it had never
actually reached `points.md`, the same gap class found earlier this
week. Recovered and logged (`#325`), directly answering the question:
no, this card has no header at any speed, the reasoning holds at both
high and low speed.

## The Tang Nano thread

Alan proposed a genuinely good, cheap idea: a stack of Sipeed Tang
Nano 20K boards (Gowin GW2AR-18, a different FPGA vendor entirely),
wired header-to-header as a testbed, opening a real question never
tested in this project's history — does "topology is computation"
hold across FPGA vendors, not just Intel devices (`#326`). Good
sourcing found and verified against Sipeed's own official datasheet
(`#327`), and real JTAG test-point details captured for later (`#328`).
A real correction made and owned directly along the way: the onboard
BL616 chip's WiFi/BLE capability was wrongly assumed usable on this
board — Sipeed's own documentation never mentions it, confirmed
independently twice over (`#326`, reinforced by `#327`'s own internal
contradiction).

## Two real, honest gaps confirmed

Checking directly against RTL and Quartus files rather than assuming:
every memory build in this project's history has only ever touched
the Arria 10's internal M20K — the on-board DDR4 has never been wired
up, `#147`'s own DDR4 discussion was always a pure throughput
calculation (`#329`). And Alan's own correct architectural point that
PCIe throughput is a host-motherboard property, not a card property,
led to recovering — again — a real decision that had never been
logged: the Dell Precision 5820 target and the reasoning behind it, a
fourth instance of the same unlogged-conversation gap pattern (`#330`).

## The full points.md audit

Alan asked for a genuine sweep through all 330 prior entries, worried
some were complete, dead, or disconnected from current work. Done
properly — every title read, grouped into five architectural eras,
cross-checked against the current NEXT lists. The honest finding:
the ledger is in genuinely good shape, nothing found that contradicts
current architecture. But three real, specific connections were found
that had drifted disconnected — most notably, `#230` (2026-08-08) had
already adopted the Tang Nano 20K with a confirmed working open-source
toolchain, information this session's own `#326` should have found
and built on before treating the idea as fresh. Owned directly rather
than glossed over. Two small genuinely orphaned items flagged for a
conscious keep/drop decision (`#331`, full detail in `docs/shared/
POINTS_STATUS_AUDIT.md`).

## The structural cleanup

Alan's real, well-founded concern: the project is entering a critical
new phase and needs to look organized to others, not like scattered
concepts across disconnected folders. A full structural audit
followed — real scatter found and documented (`pcie/` entirely dead,
two different files both named `fpga_bridge.py` neither current, a
duplicate `PAPERS.md`, a `hardware/` folder at the repo root that an
earlier triage pass had never even covered) (`#332`, full detail in
`docs/shared/STRUCTURE_AUDIT.md`).

Alan's own idea to use the project's existing Onion archival tool
(rather than risky in-place reorganization) was tested properly first
— submodule initialized, built, a real proof-of-concept pack/inspect/
search/expand cycle run and checksum-verified byte-for-byte identical.
Then a real first reorganization pass executed: four items archived,
32 files removed from the live tree, ~9,682 lines of dead/duplicate
material gone (`#333`). A real, serious bug was found and fixed during
this pass, not just theorized about: packing two files that happened
to share the same basename silently collided on extraction, only one
survived — caught specifically because every single archive got a
real checksum-and-diff verification before any live original was
deleted, exactly the discipline that was insisted on rather than
skipped as overcautious.

That same bug was then fixed properly at its actual source, in the
Onion tool's own code (not just worked around) — the auto-split
delimiter for metadata list values changed from comma to semicolon,
since commas are common in ordinary prose and were silently mangling
free-text description fields (`#334`). Pushed genuinely upstream to
the separate `github.com/alh-Imago/Onion` repo with Alan's own
credentials, hitting and fixing one more real snag along the way (a
detached-HEAD submodule state meant the first push attempt silently
targeted the wrong branch) (`#335`).

## Session close

Alan confirmed the onion tool earned its keep this session — found a
real bug, proved a real byte-for-byte-verified round-trip on real
files, not just a good pitch. The submodule init and build steps were
baked permanently into `current/START.md`'s own session-start ritual,
including the basename-collision gotcha, so neither of these needs
rediscovering next time.
